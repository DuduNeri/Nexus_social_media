import React from 'react';
import ReactDOM from 'react-dom/client';
import Main from './App'; // Ajuste o caminho se necessário
import './index.css'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Main />
  </React.StrictMode>
);

import './index.css'